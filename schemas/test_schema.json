{
  "title": "Scientific Paper",
  "description": "JSON schema for scientific research papers",
  "type": "object",
  "properties": {
    "metadata": {
      "type": "object",
      "description": "Paper metadata and bibliographic information",
      "required": ["title", "authors", "abstract"],
      "properties": {
        "title": {
          "type": "string",
          "description": "Full title of the paper"
        },
        "authors": {
          "type": "array",
          "description": "List of paper authors",
          "items": {
            "type": "object",
            "required": ["name"],
            "properties": {
              "name": {
                "type": "string",
                "description": "Author's full name"
              }
            }
          }
        },
        "abstract": {
          "type": "string",
          "description": "Paper abstract"
        }
      }
    },
    "figures": {
      "type": "array",
      "description": "Figures in the paper",
      "items": {
        "type": "object",
        "required": ["id", "caption"],
        "properties": {
          "id": {
            "type": "string",
            "description": "Figure identifier (e.g., 'Figure 1')"
          },
          "caption": {
            "type": "string",
            "description": "Figure caption"
          }
        }
      }
    },
    "tables": {
      "type": "array",
      "description": "Tables in the paper",
      "items": {
        "type": "object",
        "required": ["id", "caption", "data"],
        "properties": {
          "id": {
            "type": "string",
            "description": "Table identifier (e.g., 'Table 1')"
          },
          "caption": {
            "type": "string",
            "description": "Table caption"
          },
          "data": {
            "type": "object",
            "description": "Table structure and data",
            "required": ["headers", "rows"],
            "properties": {
              "headers": {
                "type": "array",
                "items": { "type": "string" },
                "description": "Column headers"
              },
              "rows": {
                "type": "array",
                "items": {
                  "type": "array",
                  "items": { "type": "string" },
                  "description": "Table row data"
                }
              }
            }
          }
        }
      }
    },
    "equations": {
      "type": "array",
      "description": "Mathematical equations",
      "items": {
        "type": "object",
        "required": ["id", "latex"],
        "properties": {
          "id": {
            "type": "string",
            "description": "Equation identifier"
          },
          "latex": {
            "type": "string",
            "description": "LaTeX representation of the equation"
          }
        }
      }
    },
    "experimental_setup": {
      "type": "object",
      "description": "Experimental methodology and setup",
      "required": ["description"],
      "properties": {
        "description": {
          "type": "string",
          "description": "Detailed description of experimental setup"
        }
      }
    },
    "experimental_results": {
      "type": "object",
      "description": "Experimental results and findings, including quantitative and qualitiative data",
      "required": ["quantitative_data_points, quantitative_data_points"],
      "properties": {
        "quantitative_data_points": {
          "type": "array",
          "description": "A list of all of quantitative data related to the results",
          "items": {
            "type": "object",
            "required": ["quantitative_data_point"],
            "properties": {
              "quantitative_data_point": {
                "type": "string",
                "description": "This is some concrete quantitative data from the results."
              }
            }
          }
        },
        "qualitative_data_points": {
          "type": "array",
          "description": "A list of all of qualitative data related to the results",
          "items": {
            "type": "object",
            "required": ["qualitative_data_point"],
            "properties": {
              "qualitative_data_point": {
                "type": "string",
                "description": "This is some concrete qualitative data from the results."
              }
            }
          }
        }
      }
    },
    "data_availability": {
      "type": "object",
      "description": "Information about data availability",
      "required": ["statement"],
      "properties": {
        "statement": {
          "type": "string",
          "description": "Data availability statement"
        }
      }
    },
    "code_availability": {
      "type": "object",
      "description": "Information about code availability",
      "required": ["repository_url"],
      "properties": {
        "repository_url": {
          "type": "string",
          "description": "URL to code repository"
        }
      }
    }
  }
}
